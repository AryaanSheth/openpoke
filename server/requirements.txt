# Pinned to the versions verified working on 2026-07-21.
# These were previously unbounded (`>=`), which let composio drift 0.5 -> 0.18 and
# silently break Gmail connection detection (see docs: the `.data` -> `.items` rename).
# Bump deliberately, with the test suite green — never implicitly at install time.
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
httpx==0.28.1
python-dateutil==2.9.0.post0
beautifulsoup4==4.15.0
composio==0.18.0

# Phase 0 — persistence, config, crypto. Verified resolving on Python 3.14.5.
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.18.5
pydantic-settings==2.14.2
cryptography==49.0.0
argon2-cffi==25.1.0

# Dev/test dependencies live in pyproject.toml under [project.optional-dependencies] dev.
